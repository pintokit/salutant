<%= simple_form_for(@submission) do |f| %>

  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :body %>
  <%= f.input :phone %>

  <%= f.button :submit, class: 'btn btn-block btn-success' %>
<% end %>
